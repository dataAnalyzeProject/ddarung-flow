import heroBike from "../../../assets/main/hero-bike.png";
import PlaceAutocompleteInput from "../../map/PlaceAutocompleteInput";
export default function MainSearchForm({ serviceData, input, onInputChange, onPlaceSelect, onConfirmTime, onPredict }) { return <section className="main-search"><div className="main-hero-copy"><h1>도착할 때 빌릴 수 있는<br /><em>따릉이를 미리 확인</em>하세요.</h1><p>예상 도착시간 기준 대여 가능 여부를 알려드립니다.</p></div><div className="main-fields"><PlaceAutocompleteInput label={serviceData.originLabel} value={input.origin} onChange={(value) => onInputChange("origin", value)} onSelect={(place) => onPlaceSelect("origin", place)} placeholder={serviceData.originPlaceholder} /><i aria-hidden="true">→</i><PlaceAutocompleteInput label={serviceData.destinationLabel} value={input.destination} onChange={(value) => onInputChange("destination", value)} onSelect={(place) => onPlaceSelect("destination", place)} placeholder={serviceData.destinationPlaceholder} /><label className="main-time"><span>{serviceData.expectedTimeLabel}</span><div><input aria-label={serviceData.expectedTimeLabel} type="number" min="1" value={input.directMinutes ?? ""} onChange={(event) => onInputChange("directMinutes", event.target.value === "" ? null : Number(event.target.value))} /><small>분 단위</small><button type="button" onClick={onConfirmTime}>시간 확인</button></div></label></div><div className="main-actions"><span>이동 수단</span><div className="main-mode-buttons">{serviceData.modes.map((mode) => <button className={mode === input.travelMode ? "active" : ""} key={mode} type="button" onClick={() => onInputChange("travelMode", mode)}><b className={mode === "도보" ? "walk-icon" : "transit-icon"} aria-hidden="true" />{mode}</button>)}</div><img className="main-hero-bike" src={heroBike} alt="" /><button aria-label={serviceData.predictButton} className="main-submit" type="button" onClick={onPredict}><span aria-hidden="true" />{serviceData.predictButton}</button></div></section>; }

