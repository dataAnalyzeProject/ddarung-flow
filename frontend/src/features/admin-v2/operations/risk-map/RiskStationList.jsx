function bikes(value) { return value === null || value === undefined ? '재고 확인 필요' : `${value}대`; } function probability(value) { return value === null || value === undefined ? '판단 정보 부족' : `${Math.round(value * 100)}%`; }
export default function RiskStationList({ items, selectedStationNumber, onSelect, onLoadMore, loadingMore }) { return <section className="risk-station-list" aria-labelledby="risk-list-title"><h2 id="risk-list-title">대여소 목록</h2><ol>{items.map((item) => { const number = item.station.stationNumber; return <li key={number}><button type="button" aria-current={selectedStationNumber === number ? 'true' : undefined} onClick={() => onSelect(number)}><strong>{item.station.name}</strong><span>{number}</span><span>현재 {bikes(item.station.currentBikes)}</span><span>{item.riskBand || '판단 정보 부족'} · {probability(item.rentalRisk?.selectedShortageProbability)}</span><time dateTime={item.predictionTargetAt}>{item.predictionTargetAt}</time><small>{item.dataState}</small></button></li>; })}</ol>{onLoadMore ? <button type="button" className="risk-more" onClick={onLoadMore} disabled={loadingMore}>{loadingMore ? '불러오는 중' : '더 보기'}</button> : null}</section>; }
