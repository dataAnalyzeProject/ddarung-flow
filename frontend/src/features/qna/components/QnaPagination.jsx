export default function QnaPagination({ page, onChange }) { return <nav className="qna-pagination" aria-label="질문 페이지"><button type="button" aria-label="이전 페이지" disabled={page === 1} onClick={() => onChange(Math.max(1, page - 1))}><span aria-hidden="true">‹</span></button>{[1, 2, 3].map((number) => <button type="button" aria-current={page === number ? "page" : undefined} key={number} onClick={() => onChange(number)}>{number}</button>)}<button type="button" aria-label="다음 페이지" disabled={page === 3} onClick={() => onChange(Math.min(3, page + 1))}><span aria-hidden="true">›</span></button></nav>; }
