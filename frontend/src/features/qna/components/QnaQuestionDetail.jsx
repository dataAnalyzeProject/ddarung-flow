export default function QnaQuestionDetail({ question, onBack, onEdit, onDelete }) { const isAuthor = question.authorId === "mine"; return <section className="qna-detail" aria-labelledby="qna-detail-title"><button className="qna-back" type="button" onClick={onBack}>목록으로</button><article><span className={`qna-category ${question.category.toLowerCase()}`}>{question.categoryLabel}</span><h1 id="qna-detail-title">{question.title}</h1><p>{question.visibility === "PRIVATE" ? "내 비공개" : "공개"} · {question.createdAt}</p><div>{question.body}</div>{isAuthor && <div className="qna-form-actions"><button type="button" onClick={onEdit}>수정</button><button type="button" onClick={onDelete}>삭제</button></div>}<section><h2>답변</h2><p>{question.answer || "아직 답변이 등록되지 않았습니다."}</p></section></article></section>; }

