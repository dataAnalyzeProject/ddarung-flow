# Copy this file to frontend/.env.local and replace the values locally.
# The Kakao JavaScript key is embedded in the browser bundle, so restrict its allowed domains.
REACT_APP_KAKAO_MAP_APP_KEY=
REACT_APP_API_BASE_URL=http://localhost:8080
