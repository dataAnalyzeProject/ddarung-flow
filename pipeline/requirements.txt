# Windows 로컬 단위 테스트에만 사용한다. Airflow 본체는 Docker 이미지에 포함된다.
pytest==8.4.2
# Windows Python에서 Asia/Seoul 시간대 정보를 읽기 위해 고정한다.
tzdata==2025.2
# OCI Object Storage 연결과 Instance Principal 전환에 같은 SDK를 사용한다.
oci==2.182.1
