# Local unit-test dependency. Airflow itself is provided by the Docker image.
pytest==8.4.2
# Time-zone database used by Windows Python for Asia/Seoul.
tzdata==2025.2
# OCI SDK for Object Storage and Instance Principal authentication.
oci==2.182.1
# Data processing, analysis, and Excel reading for DATA-2.0 audit
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0
# Reproducible, disk-backed aggregation for the DATA-2.0 official-source audit
duckdb==1.5.5
# Reproducible classical models for DATA-3.1
scikit-learn==1.9.0
# PostgreSQL publisher for validated DATA-3.2 prediction batches.
psycopg[binary]==3.2.10
