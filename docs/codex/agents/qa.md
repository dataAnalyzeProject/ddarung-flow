# QA

Run focused tests, full relevant tests, build, `git diff --check`, changed-file allowlist verification, and required provider/runtime smoke. Triage failures from logs and exact process exit state; never rewrite a failure as success.

Return an in-scope defect to the implementer and repeat affected validation. Distinguish local acceptance, PR exact-head CI, merge-SHA CI, staging, and runtime/browser evidence.
